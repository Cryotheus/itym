[package]
name = "itym_attack"
version = "0.1.0"
edition = "2024"

description = "Utilities intended for thin traces."
categories = []
keywords = []

license.workspace = true
repository.workspace = true

[dependencies]
itym_slot = { path = "../slot", version = "0.1.3", features = [] }

[package.metadata.docs.rs]
all-features = true
rustdoc-args = [
	"-Zunstable-options",
	"--generate-link-to-definition",
	"--generate-macro-expansion",
]
