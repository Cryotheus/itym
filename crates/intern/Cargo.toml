[package]
name = "itym_intern"
version = "0.1.0"
edition = "2024"

[dependencies]
borsh = { workspace = true, optional = true }
serde = { workspace = true, optional = true, features = ["alloc", "std"] }

[features]
default = []
borsh = ["dep:borsh"]
serde = ["dep:serde"]
